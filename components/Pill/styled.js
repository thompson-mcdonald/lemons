import styled from 'styled-components';
import { color, typography, display } from 'styled-system';

export const PillWrapper = styled.div`
  border-radius: 4px;
  padding: .2rem;
  ${color}
  ${typography}
  ${display}
`;